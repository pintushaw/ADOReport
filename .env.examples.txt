# Required
ADO_ORG=your-org-name
ADO_PROJECT=your-project-name
ADO_PAT=your-personal-access-token

# Optional — sprint & window
SPRINT_NAME=@CurrentIteration
DAYS_BACK=7
OUTPUT_DIR=./reports

# Optional — email delivery
EMAIL_TO=team@yourcompany.com,lead@yourcompany.com
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=sender@yourcompany.com
SMTP_PASS=your-smtp-password

# Optional — Microsoft Teams
TEAMS_WEBHOOK=https://yourcompany.webhook.office.com/webhookb2/...
